/**
 * Contains package-private state machine events used for state transitions. Other components are
 * not aware of these events, they are created by {@link MockClientLifecycle} and immediately
 * consumed by its {@link StateMachine} instance.
 */
package com.faforever.testharness.client.state;
